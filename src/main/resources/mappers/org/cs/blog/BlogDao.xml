<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cs.blog.dao.BlogDao">
    <select id="selectByCondiction" parameterType="java.util.Map" resultType="org.cs.blog.entity.Blog">
        select * from blog where id = #{condition.id}
    </select>
</mapper>